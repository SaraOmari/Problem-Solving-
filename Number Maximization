#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>
using namespace std; 
int main()
{
int n; cin>>n; 
vector<int>v; 

int a=n;
int b=n%10; 
while(a>=10){
    v.push_back(a%10); 
    a/=10;
}
v.push_back(a);
sort(v.rbegin(),v.rend()); 
for(int i=0; i<v.size(); i++){
   cout<<v[i]; 
}

    return 0;
}

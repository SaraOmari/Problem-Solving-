
#include <iostream>
#include <cmath>
using namespace std; 
int main()
{
int t; cin>>t; 
while(t--){
    int n; cin>>n; 
   long long arr[n]; 
   for(int i=0; i<n; i++)
   cin>>arr[i]; 
   int flag=0;
   long long  a=1,x=0;
   for(int i=0; i<n; i++){
       arr[i]+=x;
       if(arr[i]<a){
       flag=1;; 
       break;}
       else{
           x=arr[i]-a;
           a++;
       }
   }
   if(flag)
   cout<<"No\n";
   else
   cout<<"yes\n";
   
}
    return 0;
}
